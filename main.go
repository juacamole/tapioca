// Command tapioca is an agentic coding TUI. The program itself lives in
// internal/cli, so the identical binary can also be installed as `tapio` —
// see cmd/tapio.
package main

import "tapioca/internal/cli"

func main() { cli.Main() }
